- name: restart docker
  service:
    name: docker
    state: restarted
  become: yes

- name: restart omero containers
  command: docker compose restart
  args:
    chdir: "{{ ansible_user_dir }}/omero-deployment-kit/docker"
  become: yes